---
driver: { name: exec }
transport: { name: exec }

provisioner:
  product_name: cinc
  install_strategy: skip
  chef_client_path: C:\cinc-project\cinc\bin\cinc-client.bat

platforms:
  - name: windows-latest
